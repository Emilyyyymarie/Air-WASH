###########
###?????

#correct this part 

noboilingptlrsdtotal=sqrt(noboilingptlrsdchild^2+noboilingptlrsd^2)#no boiling
lablevelptlrsdtotal=sqrt(lablevelptlrsdchild^2+lablevelptlrsd^2) # lab level
verygoodptlrsdtotal=sqrt(verygoodptlrsdchild^2+verygoodptlrsd^2) #very good
moderateptlrsdtotal=sqrt(moderateptlrsdchild^2+moderateptlrsd^2) # moderate
goodptlrsdtotal=sqrt(goodptlrsdchild^2+goodptlrsd^2) #good
loweffectiveptlrsdtotal=sqrt(loweffectiveptlrsdchild^2+loweffectiveptlrsd^2) # low effective
ineffectiveptlrsdtotal=sqrt(ineffectiveptlrsdchild^2+ineffectiveptlrsd^2)#ineffecctive
negativeptlrsdtotal=sqrt(negativeptlrsdchild^2+negativeptlrsd^2)#negative boiling

noboilingptmrsdtotal=sqrt(noboilingptmrsdchild^2+noboilingptmrsd^2)#no boiling
lablevelptmrsdtotal=sqrt(lablevelptmrsdchild^2+lablevelptmrsd^2) # lab level
verygoodptmrsdtotal=sqrt(verygoodptmrsdchild^2+verygoodptmrsd^2) #very good
moderateptmrsdtotal=sqrt(moderateptmrsdchild^2+moderateptmrsd^2) # moderate
goodptmrsdtotal=sqrt(goodptmrsdchild^2+goodptmrsd^2) #good
loweffectiveptmrsdtotal=sqrt(loweffectiveptmrsdchild^2+loweffectiveptmrsd^2) # low effective
ineffectiveptmrsdtotal=sqrt(ineffectiveptmrsdchild^2+ineffectiveptmrsd^2)#ineffecctive
negativeptmrsdtotal=sqrt(negativeptmrsdchild^2+negativeptmrsd^2)#negative boiling

noboilingpthrsdtotal=sqrt(noboilingpthrsdchild^2+noboilingpthrsd^2)#no boiling
lablevelpthrsdtotal=sqrt(lablevelpthrsdchild^2+lablevelpthrsd^2) # lab level
verygoodpthrsdtotal=sqrt(verygoodpthrsdchild^2+verygoodpthrsd^2) #very good
moderatepthrsdtotal=sqrt(moderatepthrsdchild^2+moderatepthrsd^2) # moderate
goodpthrsdtotal=sqrt(goodpthrsdchild^2+goodpthrsd^2) #good
loweffectivepthrsdtotal=sqrt(loweffectivepthrsdchild^2+loweffectivepthrsd^2) # low effective
ineffectivepthrsdtotal=sqrt(ineffectivepthrsdchild^2+ineffectivepthrsd^2)#ineffecctive
negativepthrsdtotal=sqrt(negativepthrsdchild^2+negativepthrsd^2)#negative boiling

######################################################
#diff total
#correct this part 

noboilingptlrsdtotal=sqrt(noboilingptlrsdchild^2+noboilingptlrsd^2)#no boiling
lablevelptlrsdtotal=sqrt(lablevelptlrsdchild^2+lablevelptlrsd^2) # lab level
verygoodptlrsdtotal=sqrt(verygoodptlrsdchild^2+verygoodptlrsd^2) #very good
moderateptlrsdtotal=sqrt(moderateptlrsdchild^2+moderateptlrsd^2) # moderate
goodptlrsdtotal=sqrt(goodptlrsdchild^2+goodptlrsd^2) #good
loweffectiveptlrsdtotal=sqrt(loweffectiveptlrsdchild^2+loweffectiveptlrsd^2) # low effective
ineffectiveptlrsdtotal=sqrt(ineffectiveptlrsdchild^2+ineffectiveptlrsd^2)#ineffecctive
negativeptlrsdtotal=sqrt(negativeptlrsdchild^2+negativeptlrsd^2)#negative boiling

noboilingptmrsdtotal=sqrt(noboilingptmrsdchild^2+noboilingptmrsd^2)#no boiling
lablevelptmrsdtotal=sqrt(lablevelptmrsdchild^2+lablevelptmrsd^2) # lab level
verygoodptmrsdtotal=sqrt(verygoodptmrsdchild^2+verygoodptmrsd^2) #very good
moderateptmrsdtotal=sqrt(moderateptmrsdchild^2+moderateptmrsd^2) # moderate
goodptmrsdtotal=sqrt(goodptmrsdchild^2+goodptmrsd^2) #good
loweffectiveptmrsdtotal=sqrt(loweffectiveptmrsdchild^2+loweffectiveptmrsd^2) # low effective
ineffectiveptmrsdtotal=sqrt(ineffectiveptmrsdchild^2+ineffectiveptmrsd^2)#ineffecctive
negativeptmrsdtotal=sqrt(negativeptmrsdchild^2+negativeptmrsd^2)#negative boiling

noboilingpthrsdtotal=sqrt(noboilingpthrsdchild^2+noboilingpthrsd^2)#no boiling
lablevelpthrsdtotal=sqrt(lablevelpthrsdchild^2+lablevelpthrsd^2) # lab level
verygoodpthrsdtotal=sqrt(verygoodpthrsdchild^2+verygoodpthrsd^2) #very good
moderatepthrsdtotal=sqrt(moderatepthrsdchild^2+moderatepthrsd^2) # moderate
goodpthrsdtotal=sqrt(goodpthrsdchild^2+goodpthrsd^2) #good
loweffectivepthrsdtotal=sqrt(loweffectivepthrsdchild^2+loweffectivepthrsd^2) # low effective
ineffectivepthrsdtotal=sqrt(ineffectivepthrsdchild^2+ineffectivepthrsd^2)#ineffecctive
negativepthrsdtotal=sqrt(negativepthrsdchild^2+negativepthrsd^2)#negative boiling

#################################################################################
noboilingptlrsdtotal=sqrt(noboilingptlrsdchild^2+noboilingptlrsd^2)#no boiling
lablevelptlrsdtotal=sqrt(lablevelptlrsdchild^2+lablevelptlrsd^2) # lab level
verygoodptlrsdtotal=sqrt(verygoodptlrsdchild^2+verygoodptlrsd^2) #very good
moderateptlrsdtotal=sqrt(moderateptlrsdchild^2+moderateptlrsd^2) # moderate
goodptlrsdtotal=sqrt(goodptlrsdchild^2+goodptlrsd^2) #good
loweffectiveptlrsdtotal=sqrt(loweffectiveptlrsdchild^2+loweffectiveptlrsd^2) # low effective
ineffectiveptlrsdtotal=sqrt(ineffectiveptlrsdchild^2+ineffectiveptlrsd^2)#ineffecctive
negativeptlrsdtotal=sqrt(negativeptlrsdchild^2+negativeptlrsd^2)#negative boiling

noboilingptmrsdtotal=sqrt(noboilingptmrsdchild^2+noboilingptmrsd^2)#no boiling
lablevelptmrsdtotal=sqrt(lablevelptmrsdchild^2+lablevelptmrsd^2) # lab level
verygoodptmrsdtotal=sqrt(verygoodptmrsdchild^2+verygoodptmrsd^2) #very good
moderateptmrsdtotal=sqrt(moderateptmrsdchild^2+moderateptmrsd^2) # moderate
goodptmrsdtotal=sqrt(goodptmrsdchild^2+goodptmrsd^2) #good
loweffectiveptmrsdtotal=sqrt(loweffectiveptmrsdchild^2+loweffectiveptmrsd^2) # low effective
ineffectiveptmrsdtotal=sqrt(ineffectiveptmrsdchild^2+ineffectiveptmrsd^2)#ineffecctive
negativeptmrsdtotal=sqrt(negativeptmrsdchild^2+negativeptmrsd^2)#negative boiling

noboilingpthrsdtotal=sqrt(noboilingpthrsdchild^2+noboilingpthrsd^2)#no boiling
lablevelpthrsdtotal=sqrt(lablevelpthrsdchild^2+lablevelpthrsd^2) # lab level
verygoodpthrsdtotal=sqrt(verygoodpthrsdchild^2+verygoodpthrsd^2) #very good
moderatepthrsdtotal=sqrt(moderatepthrsdchild^2+moderatepthrsd^2) # moderate
goodpthrsdtotal=sqrt(goodpthrsdchild^2+goodpthrsd^2) #good
loweffectivepthrsdtotal=sqrt(loweffectivepthrsdchild^2+loweffectivepthrsd^2) # low effective
ineffectivepthrsdtotal=sqrt(ineffectivepthrsdchild^2+ineffectivepthrsd^2)#ineffecctive
negativepthrsdtotal=sqrt(negativepthrsdchild^2+negativepthrsd^2)#negative boiling

######################################################
#correct this part 

noboilingptlrsdtotal=sqrt(noboilingptlrsdchild^2+noboilingptlrsd^2)#no boiling
lablevelptlrsdtotal=sqrt(lablevelptlrsdchild^2+lablevelptlrsd^2) # lab level
verygoodptlrsdtotal=sqrt(verygoodptlrsdchild^2+verygoodptlrsd^2) #very good
moderateptlrsdtotal=sqrt(moderateptlrsdchild^2+moderateptlrsd^2) # moderate
goodptlrsdtotal=sqrt(goodptlrsdchild^2+goodptlrsd^2) #good
loweffectiveptlrsdtotal=sqrt(loweffectiveptlrsdchild^2+loweffectiveptlrsd^2) # low effective
ineffectiveptlrsdtotal=sqrt(ineffectiveptlrsdchild^2+ineffectiveptlrsd^2)#ineffecctive
negativeptlrsdtotal=sqrt(negativeptlrsdchild^2+negativeptlrsd^2)#negative boiling

noboilingptmrsdtotal=sqrt(noboilingptmrsdchild^2+noboilingptmrsd^2)#no boiling
lablevelptmrsdtotal=sqrt(lablevelptmrsdchild^2+lablevelptmrsd^2) # lab level
verygoodptmrsdtotal=sqrt(verygoodptmrsdchild^2+verygoodptmrsd^2) #very good
moderateptmrsdtotal=sqrt(moderateptmrsdchild^2+moderateptmrsd^2) # moderate
goodptmrsdtotal=sqrt(goodptmrsdchild^2+goodptmrsd^2) #good
loweffectiveptmrsdtotal=sqrt(loweffectiveptmrsdchild^2+loweffectiveptmrsd^2) # low effective
ineffectiveptmrsdtotal=sqrt(ineffectiveptmrsdchild^2+ineffectiveptmrsd^2)#ineffecctive
negativeptmrsdtotal=sqrt(negativeptmrsdchild^2+negativeptmrsd^2)#negative boiling

noboilingpthrsdtotal=sqrt(noboilingpthrsdchild^2+noboilingpthrsd^2)#no boiling
lablevelpthrsdtotal=sqrt(lablevelpthrsdchild^2+lablevelpthrsd^2) # lab level
verygoodpthrsdtotal=sqrt(verygoodpthrsdchild^2+verygoodpthrsd^2) #very good
moderatepthrsdtotal=sqrt(moderatepthrsdchild^2+moderatepthrsd^2) # moderate
goodpthrsdtotal=sqrt(goodpthrsdchild^2+goodpthrsd^2) #good
loweffectivepthrsdtotal=sqrt(loweffectivepthrsdchild^2+loweffectivepthrsd^2) # low effective
ineffectivepthrsdtotal=sqrt(ineffectivepthrsdchild^2+ineffectivepthrsd^2)#ineffecctive
negativepthrsdtotal=sqrt(negativepthrsdchild^2+negativepthrsd^2)#negative boiling

#################################################################################
